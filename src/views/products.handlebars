<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles.css">
    <script src="script.js" defer></script>
    <script src="https://kit.fontawesome.com/a076d05399.js"></script>
    <title>Mi Aplicación</title>
    <style>

    </style>
</head>
<body>
    <div class="navbar">
        <h1>Bienvenido {{user.first_name}}</h1>
        <button class="hamburger-btn" onclick="toggleMenu()">☰</button>
        <div class="menu-panel" id="menuPanel">
            <div class="user-menu">
                <div class="user-details">
                    <i class="fas fa-user"></i>
                    <span>Nombre de usuario: {{user.first_name}}</span>
                </div>
                <div class="user-details">
                    <i class="fas fa-user"></i>
                    <span>Apellido de usuario: {{user.last_name}}</span>
                </div>
                <div class="user-details">
                    <i class="fas fa-user"></i>
                    <span>Edad de usuario: {{user.age}}</span>
                </div>
                <div class="user-details">
                    <i class="fas fa-envelope"></i>
                    <span>Correo electrónico: {{user.email}}</span>
                </div>
            </div>
        </div>
    </div>

    <h2>Lista de Productos</h2>

<h1 style="display: flex; justify-content: center;"> Bienvenido {{user.first_name}} </h1>


<h2 style="display: flex; justify-content: center;">Lista de Productos</h2>
<div></div>

<table class="table table-striped table-bordered">
    <thead class="bg-primary text-white">
        <tr>
            <th scope="col">Código</th>
            <th scope="col">Título</th>
            <th scope="col">Descripción</th>
            <th scope="col">Precio</th>
            <th scope="col">Stock</th>
            <th scope="col">Categoría</th>

        </tr>
    </thead>
    <tbod>
        {{#each productsMap}}
        <tr>
            <td>{{this.code}}</td>
            <td>{{this.title}}</td>
            <td>{{this.description}}</td>
            <td>{{this.price}}</td>
            <td>{{this.stock}}</td>
            <td>{{this.category}}</td>

        </tr>
        {{/each}}
    </tbod>
</table>

<div class="d-flex justify-content-center mt-3">
    {{#if hasPrevPage}}
    <a class="btn btn-primary btn-block mb-3" href="{{prevPage}}">Anterior</a>
    {{/if}}

    {{#if hasNextPage}}
    <a class="btn btn-primary btn-block mb-3" href="{{nextPage}}">Siguiente</a>
    {{/if}}
</div>



<div class="d-flex justify-content-center mt-3">
    <p>Página {{page}} de {{totalPages}}</p>
</div>


<section style="display: flex; justify-content: center" class="mt-4">
    <button class="btn btn-primary btn-block mb-3" onclick="location.href='/logout'">Cerrar sesion</button>
</section>

<script type='text/javascript'>
function toggleMenu() {
    var menuPanel = document.getElementById("menuPanel");
    if (menuPanel.style.display === "none") {
        menuPanel.style.display = "block";
    } else {
        menuPanel.style.display = "none";
    }
}
</script>

</body>
</html>